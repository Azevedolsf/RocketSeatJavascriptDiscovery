

const pao = true;
const queijo = true;

const niceBreakFast = pao && queijo ? "Café da manha top" : "café da manha ok";

console.log(niceBreakFast);


let age = 16;

const canDrive = age >= 18 ? "Pode Dirigir" : "Menor de idade ! Não pode digirir"

console.log(canDrive);